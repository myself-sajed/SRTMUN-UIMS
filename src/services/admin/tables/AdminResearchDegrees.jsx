import React from 'react'
// import AdminPrototype from '../AdminPrototype';

const AdminResearchDegrees = () => {


    return (
        // <AdminPrototype model="Degree" title="Research Degrees" >


            <>

            {/* <div> data disayalay</div>

                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Full Name</th>
                        <th scope="col">Degrees</th>
                        <th scope="col">Title</th>
                        <th scope="col">University</th>
                        <th scope="col">Award Date</th>
                        <th scope="col">Award Letter</th>
                    </tr>
                </thead>
                <tbody>

                    {
                        data && data.sort(function (a, b) {
                            var dateA = new Date(a.createdAt), dateB = new Date(b.createdAt)
                            return dateB - dateA;
                        }).map((item, index) => {
                            return (
                                <tr key={index}>
                                    <th scope="row">{index + 1}</th>
                                    <td>{item.userId && item.userId.name}</td>
                                    <td>{item.degreeName}</td>
                                    <td>{item.title}</td>
                                    <td>{item.university}</td>
                                    <td>{item.awardDate}</td>
                                    <td><a item={item.degree} className='cursor-pointer hover:text-blue-900 text-blue-600'>View Degree</a></td>
                                </tr>

                            )
                        })
                    }


                </tbody> */}

            </>

        // </AdminPrototype >

    )
}

export default AdminResearchDegrees