const generateReport = (aqarData, selectedYear, setReportLoading) => {
    console.log('AQAR Data : ', aqarData, 'For year :', selectedYear);

    setReportLoading(false)
}

export default generateReport;